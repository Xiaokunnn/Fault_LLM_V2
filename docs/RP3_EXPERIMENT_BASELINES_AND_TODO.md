# 研究点三实验基线与TODO

更新日期：2026-09-18

本文件把“已经执行的基线”“硕士论文最少需要完成的对照”和“可选扩展”分开。方法总设计仍以
`docs/research/MASTER_THESIS_THREE_RESEARCH_POINTS_PLAN.md`为准，当前运行状态以
`docs/RESEARCH_HANDOFF.md`为准。

2026-09-17续跑：P0增强实验已完成并校准失败关闭；P1离线诊断及R0–R3重放已完成。
逐头消融已按 [`RP3_HEAD_ABLATION_V1_PROTOCOL.md`](RP3_HEAD_ABLATION_V1_PROTOCOL.md) 完成三种子A1–A4，不改变B0/M0。
字段监督审计及三种子归一化对照完成：欠填与Pointer F1改善，但新增教师空集误填，整体改善条件未通过；
见[`RP3_FIELD_NORMALIZATION_V1_RESULTS_20260917.md`](RP3_FIELD_NORMALIZATION_V1_RESULTS_20260917.md)。
联合门控审计与W1/J1三种子实验也已完成：误填降至0但F1明显下降，整体优化失败；
见[`RP3_JOINT_CONTRACT_V1_RESULTS_20260917.md`](RP3_JOINT_CONTRACT_V1_RESULTS_20260917.md)。
支持可分性/梯度审计及G1正支持更新保护完成：相对W1改善，但未超过C0，正负支持同时饱和；
见[`RP3_POSITIVE_GUARD_V1_RESULTS_20260917.md`](RP3_POSITIVE_GUARD_V1_RESULTS_20260917.md)。
S0/S1集合上下文与容量控制完成，validation选集均未变化，停止当前哈希四头结构/损失试探；
见[`RP3_SET_CONTEXT_V1_RESULTS_20260917.md`](RP3_SET_CONTEXT_V1_RESULTS_20260917.md)。
E1语义特征与同维度H512三种子对照也已完成，两项总体比较失败，见
[`RP3_SEMANTIC_V1_RESULTS_20260918.md`](RP3_SEMANTIC_V1_RESULTS_20260918.md)。
暂停本轮encoder搜索及依赖其改善的新路由训练；选择性执行与资源自适应边云协同仍是待论证候选，
不是已验证的替代主方法，见[`范围评审`](research/RP3_SCOPE_REASSESSMENT_20260917.md)。
可复查入口：[`../results/experiments/research_point_3/diagnostic_comparison_v1/comparison.md`](../results/experiments/research_point_3/diagnostic_comparison_v1/comparison.md)。

## 1. 实验问题与固定边界

研究点三回答：能否把冻结RP2教师的候选排序、直接支持、字段欠填和三动作路由压缩为小于50M参数的
LEC，使其在有界本地证据记忆上减少BGE检索、候选重排、长提示和7B核验调用，同时保持证据ID闭包并在
不可靠时回退或拒答。

固定边界：已知故障范围和诊断角色；严格208为唯一主教师；MP008只做量化后阈值校准；MP009–MP013
只做最终外部评价；教师一致性不是专家诊断准确率；服务器GPU结果不是边缘实机结果。

## 2. 基线与主方法定义

| ID | 实验臂 | 用途 | 当前状态 |
|---|---|---|---|
| T0 | RP2 Full K3 + Qwen2.5-7B教师 | 教师质量/计算开销参考上界；不是学生模型 | 已冻结并重放 |
| B0 | `LEC-Base-Hash`：字符哈希特征、四头联合、无证据移除增强 | 无干预蒸馏基线 | 已训练、路由、ONNX、INT8；MP008校准失败 |
| M0 | `LEC-Aug-Hash`：与B0同架构同超参数，加预先声明的构建集证据移除和冻结教师重放 | 当前主方法实验臂，检验干预监督是否改善欠填和路由泛化 | 165条轨迹；训练/路由/INT8完成；MP008全弃答、校准失败 |
| R0 | Always-local：忽略Route头，只要有满足当前指针约束的非空提案就本地回答，否则弃答 | 路由收益下界/危险接受对照 | 已在B0/M0的FP32和INT8输出上离线重放；不是事实安全保证 |
| R1 | Always-teacher：全部调用RP2教师 | 教师调用成本上界和质量参考 | 已重放冻结教师结果，实际7B调用为0；尚非端到端计时 |
| R2 | Always-abstain | 零覆盖下界，防止用低覆盖伪装低风险 | 已汇总；零回答时条件风险为NA |
| R3 | 简单置信度/熵阈值路由 | 检验价值路由与简单不确定性门控的差异 | 已重放固定0.8最小支持置信度/0.5归一化熵门槛，未拟合 |
| A1 | Rank-only | 检验只有排序蒸馏是否足够 | 三种子完成；原始validation非空精确成功2/8、2/8、2/8；无支持契约，不可部署 |
| A2 | Rank + Support | 测量二值支持头的增益 | 三种子完成；原始validation成功2/8、3/8、2/8 |
| A3 | Rank + Support + Field/Underfill | 测量字段状态、STOP和主动欠填的增益 | 三种子完成；原始validation成功均0/8，数量预测退化仍存在 |
| A4 | Full four-head + cost-sensitive Route | 完整LEC，与A1–A3形成逐模块消融链 | 三种子完成；复用A3 bootstrap，仅拟合Route，指针逐条保持一致 |
| E0 | 当前256/288维字符n-gram哈希编码器 | 零可学习编码开销的轻量特征基线 | 已实现 |
| E1 | 冻结BGE-small-zh-v1.5 + 同维度LEC | 固定记忆、组隔离查询泛化内部对照；包含编码器全成本 | 三种子完成，总参数25,411,621；相对C0/H512总体改善均失败 |
| Q0 | FP32 ONNX | 量化前质量、模型大小和时延参考 | B0/M0已汇总构建集质量、字节数、服务器CPU控制器p50/p95 |
| Q1 | INT8 QDQ、未通过重校准 | 只用于量化敏感性诊断，不是可部署方案 | B0/M0已完成诊断；全构建集分别有1/5条选集变化 |
| Q2 | INT8 QDQ + MP008后量化校准 | 主部署候选 | B0/M0均失败，无部署证书 |

`B0`必须保留为正式负结果，不能删除后只报告增强组。`M0`是预先规划的增强对照，不得依据MP008结果修改
其架构、训练超参数或早停规则。3B INT4闭卷、3B工具壳和3B答案级LoRA-KD属于可选扩展，不是当前
LEC主结论成立的前置条件。

## 3. 硕士论文最少实验矩阵

必须完成：

1. T0教师、B0无增强LEC、M0干预增强LEC三组对照；
2. A1→A2→A3→A4逐头消融，至少固定同一数据、同一特征和同一分组；
3. R0/R1/R2/R3与成本敏感Route策略比较；
4. FP32与INT8的模型大小、输出偏差、质量和时延比较，INT8必须重新校准；
5. 构建集组隔离validation内部诊断、MP008校准结果、MP009–MP013冻结后外部评价；
6. 参数量、模型/记忆体积、控制器p50/p95、回退率、教师调用节省和端到端成本；
7. 至少3个预声明随机种子，报告基础故障场景级结果与变异，不能把派生干预行当独立样本计算显著性；
8. 至少一块目标边缘设备的内存、端到端时延、能耗和30分钟热稳态。没有设备时只能报告资源受限仿真。

E1与同维度哈希控制H512、历史C0比较已经完成；该单一候选未取得整体增益。
这不证明所有语义编码器无效，也不证明哈希方案最优；按预设停止本轮表示搜索，避免在同一小验证集持续试参。

## 4. 统一指标

质量/契约：Pointer P/R/F1和NDCG、Support F1/AUPRC/ECE、字段状态与基数准确率、空字段准确率、
教师集合精确一致率、不可用ID率、证据引用闭包率、证据删除方向一致性。

选择性执行：answer覆盖率、回答条件下教师不一致率、风险—覆盖曲线/AURC、三动作混淆、Route regret、
fallback/abstain率。Always-abstain必须与其他策略同时报告，避免低覆盖被误解为高质量。

系统：参数量、FP32/INT8字节数、证据记忆字节数、控制器和完整工具链p50/p95、峰值内存、教师调用次数、
提示Token节省；能耗与热稳定只在真实目标硬件上报告。

所有质量指标默认是相对冻结教师或自动契约的指标，不得写成事实正确率、诊断准确率或工程安全率。

## 5. TODO与依赖顺序

### P0：完成当前增强实验

- [x] 在服务器完成augment，保留125条派生轨迹及所有7B响应和断点；不重复运行。
- [x] 使用既定增强配置与独立目录完成train、route、export、quantize。
- [x] 用MP008执行calibrate并保存失败报告；**evaluate仍阻塞**。
- [x] 汇总B0/M0构建集一致性、动作分布、既有MP008风险—覆盖网格及量化偏差；见统一比较产物。

### P1：补齐不依赖部署证书的离线诊断

- [x] 增加独立`diagnose`入口，读取现有FP32/INT8，报告Pointer、Support、Field和Route原始指标。
- [x] 标记为内部诊断/不可部署；不改evaluate门槛、不读取MP008或外部样本、不训练。
- [x] 生成逐行JSONL、统一JSON/CSV/Markdown，并校验模型、特征、教师和比较子集身份。

### P2：核心消融和路由基线

- [x] 固化A1–A4协议、三种子、输出目录和解码规则，见`head_ablation_v1.json`及独立协议文档。
- [x] 在同一学生输出上重放R0–R3，统一无量纲成本1/8/12/50；不伪装实测教师成本。
- [x] A1–A4使用7042027/7042028/7042029，按基础场景聚合并报告种子均值、SD、描述性95% t区间和失败数；B0/M0主方法三种子扩展尚未运行，不能借此冒充完成。
- [ ] 增加去可用性掩码/去干预监督消融，但不可允许未知或不可用ID进入输出。

### P2a：字段监督、损失与早停诊断

- [x] 统计请求/非请求字段零标签，分别核查原始/派生、train/validation及实际CE贡献；未把标签占比等同梯度主导。
- [x] 审计A1/A2/A3全部epoch历史；验证排序损失增长压过其他损失下降，旧模型后续任务指标未保存，不能反推。
- [x] 执行前三种子固定全字段vs请求/非请求分别归一化协议；复用A3控制，新臂独立配置和目录，保持其余设置。
- [x] 报告raw零预测、请求基数、Pointer P/R/F1及两类空证据误填，按场景/种子汇总；不以回答数替代质量。
- [x] 保存负面判断：7042027新增3/12教师空集误填，未满足全种子约束；不推广该臂，不改选epoch。
- [x] 对有可用候选但教师空集的支持/字段/数量门控进行联合诊断，保留oracle替换仅为定位的边界。
- [ ] 将排序损失与任务质量的模型选择一致性单独研究，不能直接按本轮后期F1事后选checkpoint。

### P2b：具有可验证理论边界的联合契约候选

- [x] 推导支持数量/字段状态/输出数量的可行概率、O(|A|K)递推、KL投影解释、梯度和困难空集代理风险界；说明一致性不保证正确的反例。
- [x] 固定W1困难负例监督与J1额外联合正则的三种子单因素链，复用旧C0；未改教师、输入、解码、Route或早停规则。
- [x] 验证概率穷举、梯度与掩码边界，独立重建六次训练总损失；123项测试通过，1265个旧文件保留。
- [x] 保留负结果：W1/J1相对C0的原始validation F1为0.0750 vs 0.3472；零误填来自明显覆盖损失，不能宣称整体优化。
- [x] 区分J1/W1的raw指标及概率一致性改善与最终输出质量：三个种子的validation选集逐条相同。
- [x] 完成正/负支持可分性、点式输入标签冲突和全train局部梯度审计；不能把冲突组或局部方向当作唯一退化原因。

### P2c：正支持更新约束与观测边界

- [x] 固定G1三种子及C0/W1只读控制；实际AdamW位移半空间投影、参考损失核验、失败恢复，预声明理论及适用边界。
- [x] 验证投影、回溯/拒绝及状态恢复、掩码与RNG；129项RP3测试通过，独立重建训练目标和162步保护记录，1331个旧文件保留。
- [x] 同时报告G1/W1通过和G1/C0失败：F1均值0.1972/0.0750/0.3472，G1空集误填每种子0/12；不只与退化的W1比较。
- [x] 记录负面机制：G1正支持20/20和负支持15/15同时过门；全train正支持CE单调下降不保证判别能力改善。
- [x] 完成候选集合上下文S1及同容量S0控制，共六次训练；初始化、排列等变、可用性屏蔽及实际冲突组响应已核验。
- [x] 保存S1/C0、S1/S0两项失败；全部val选集未改变，按停止规则暂停本条结构/损失试探。
- [ ] 双侧代理风险预算保留为暂停的备选；如恢复须另立协议，不能就地改G1参数或checkpoint。

### P2d：研究范围收窄与可行性门槛

- [x] 只读计算固定提案的oracle回退下界`P−G`，用枚举策略验证；C0/S1原始val至少需要5/5/7次完整教师回退。
- [x] 完成范围评审，区分已实现负结果和新的选择性执行候选；137项测试通过，1388个旧文件保留。
- [x] 完成P3单个预注册E1可行性对照；相对C0未增加正确提案，暂不训练依赖其改善的新风险/价值估计器。
- [ ] 先评审任务监督、独立构建场景和论文贡献边界；形成新数据/评价协议后再决定是否恢复学习型方法。
- [ ] 存在可利用正确提案后，预声明分组交叉拟合、资源条件化三动作控制及始终本地/教师/拒答基线。
- [ ] 资源成本、服务覆盖和教师相对风险同时评价；不通过全弃答或全量教师回退冒充优化。
- [ ] MP008校准不可行问题另立评审，不更改原样本、旧门槛或证书状态。

### P3：特征泛化补强

- [x] 预先固定BGE-small-zh-v1.5版本/文件SHA，连同LEC共25,411,621参数；H512控制配对维度与初始化。
- [x] 只用build-train训练、build-validation早停，三种子完成，不读取MP008或外部集作选择。
- [x] 计入编码器完整参数、FP32字节、动态查询编码和进程RSS；40条原始查询在同一CPU后端测组件成本。
- [x] 报告C0/H512/E1的F1为0.3472/0.2819/0.2792，误填与精确提案门槛失败，按预设停止本轮encoder搜索。
- [ ] 只有可行候选冻结后才考虑量化与MP008校准；当前E1未获晋级，原校准门槛不可行事实不变。

### P4：冻结后评价

- [x] 完成B0/M0严格208内部诊断表，明确原始/派生/合并子集及未校准边界；这不是最终独立评价。
- [ ] 在不改阈值/配置的前提下运行TierShift-620和TierShift-1326压力测试；
- [ ] 最后一次性运行MP009–MP013外部评价，之后不再据其结果调参；
- [ ] 报告外部评价的文档、来源族、基础场景数，不把证据条目数当独立病例数。

### P5：边缘部署与系统测量

- [ ] 冻结ONNX、INT8、证据记忆、特征编码器和阈值的联合哈希；
- [ ] 接入可选对话壳时，保证它只能调用工具和转述证据卡，不能新增无证据原子主张；
- [ ] 在Jetson Orin Nano/NX或明确的目标设备上测量完整链路，而非只测ORT控制器；
- [ ] 完成30分钟稳态、能耗、峰值内存和fallback网络成本测试。

### P6：可选扩展

- [ ] 3B INT4闭卷、3B+工具、3B答案级LoRA-KD；
- [ ] 候选宽度8/16/32、传统逻辑回归/GBDT、四个独立头模型；
- [ ] Full教师与NoGraph教师配对蒸馏。若差异不显著，不使用“图拓扑蒸馏”表述。

## 6. 决策门槛

当前M0已失败。另有结构性校准阻塞：MP008只有3条教师非空回答，在禁止空集answer的契约下，
至少5个接受答案必有至少40%教师集合不一致率，故现有“5个/10%”门槛不可达。
保留该事实，不能根据MP008去改学生超参数或降低门槛；校准协议问题须与构建集模型改进分别处理。

当前可执行优先级：字段归一化、W1/J1/G1、S0/S1及E1/H512均已完成；当前结构/损失/encoder试探暂停。
下一步先审查构建集任务、监督和独立组数，以及可独立于RP2成立的方法贡献；不因连续负结果直接开启新路由训练。
不叠加失败处理、不放宽阈值；排序损失/模型选择一致性与剩余消融仍为独立待办。
A1/A2并非安全部署替代品，不能因为指针分数更高就绕过欠填/支持契约。
当前A3/A4原始validation场景宏Pointer F1为0.0556±0.0481，A1为0.5167±0.0417、A2为0.5111±0.0747；
validation仅2个故障场景，不能据此宣称普遍优势。完整产物见
[`../results/experiments/research_point_3/head_ablation_v1/summary.md`](../results/experiments/research_point_3/head_ablation_v1/summary.md)。
由于复合辅助项在消融链中统一关闭，A4不是M0复跑；本轮不据MP008进行新模型/阈值选择。

- 若M0通过MP008门槛：冻结其配置和阈值，进入核心消融与外部评价；
- M0、增强和E1跟进均未通过整体门槛：保留失败，不降低门槛；恢复新实验前先固定任务/数据/评价修订及可证伪的贡献主张；
- 任何新模型设计只能由构建集train/validation驱动。MP008已经用于校准审计，不能成为模型选择依据；
- 在MP009–MP013运行前冻结最终代码、模型、阈值、评估脚本和清单哈希；
- 没有有效校准证书和目标硬件测量时，只能称“可量化的边缘控制器原型”，不能称“已完成边缘部署”。
