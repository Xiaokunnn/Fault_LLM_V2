# Fault_llm_v2 agent handoff

## Current research structure

- Research point 1 is automated evidence-quality gating and traceable knowledge-graph construction without triple-by-triple human approval.
- Research point 2 is budget-constrained hybrid evidence selection and verifier-workload control. One-hop graph propagation is a tail-recall signal, not the main source of mean improvement.
- Research point 3 is a callable Lightweight Evidence Controller (LEC) that distills ranking, binary support, active underfilling, and cost-sensitive routing into a small edge tool.
- After the failed matched-capacity set-context experiment, the user authorized considering a narrower RP3 scope. The current hash-feature four-head optimization chain is paused. The candidate next direction is evidence-contract-preserving selective execution and resource-adaptive edge/cloud collaboration; it is a research plan, not a validated replacement. See `docs/research/RP3_SCOPE_REASSESSMENT_20260917.md`.
- The active implementation target is research point 3. Research points 1 and 2 are frozen upstream teachers and must not be silently changed to improve RP3.
- The object remains the ship engine-room pump system. The sibling `Edge_Fault_LLM` directory is read-only prototype reference.

## Sources of truth

- RP1 claims and numbers come first from `papers/ICSMD_2026/RP1_ICSMD2026_English_Final.tex` and its frozen experiment assets.
- RP2 claims and numbers come first from `papers/D2AI_ICDM_2026/RP2_D2AI2026_English_Final_v1.tex`, `docs/RP2_V6_EQUAL_BUDGET_PROTOCOL.md`, and `configs/frozen/rp2_v6_paper_evidence_freeze.json`.
- The current three-point plan is `docs/research/MASTER_THESIS_THREE_RESEARCH_POINTS_PLAN.md`.
- The current RP3 comparison matrix and ordered backlog are `docs/RP3_EXPERIMENT_BASELINES_AND_TODO.md`.
- Files under `docs/archive/` and old v1-v5 protocols are historical reproduction records, not current project status.

## Terminology and evidence boundary

- Do not use “Silver” as the current research-point name, claimed innovation, or shorthand for truth quality.
- Use: candidate record, automatically evidence-qualified record, quarantined record, rejected record, automatic benchmark label, or teacher-generated supervision.
- Historical filenames, directories, schemas, status codes, paper text, and frozen configs containing `silver` remain unchanged for reproducibility.
- Automatically evidence-qualified means only that a record passed the declared executable evidence contract. It is not expert-confirmed factual accuracy, diagnostic accuracy, or an engineering safety guarantee.
- No triple-by-triple domain-expert review has been performed. Never call automatic records Gold or human ground truth.
- The canonical semantic graph is Chinese. Source-language surfaces, verbatim evidence, physical PDF page, URL, document/page hashes, scope, and all location/provenance fields that actually exist must remain unchanged. The current strict-208 source records contain character offsets but no bbox; preserve and disclose that absence instead of inventing boxes or claiming complete bbox retention.

## Frozen corpus split

- Primary graph build set: MP001–MP007 and MP015–MP022 (15 documents, 1934 physical pages).
- Development only: MP008. It must not fill build coverage or enter the primary graph.
- External evaluation only: MP009–MP013. They must not enter the primary graph, RP3 training, curriculum design, prompt/schema tuning, thresholds, retrieval parameters, or router calibration.
- MP010–MP013 were processed once after upstream freezing and remain external evaluation assets only.
- MP014 is excluded because it is an offshore ESP signal dataset rather than a ship engine-room pump document-evidence source.

## Current frozen assets

- RP1 full audit: 8003 records = 1698 automatically evidence-qualified, 881 quarantined, 5424 rejected.
- RP1 terminology layers: strict 208 evidence records, conservative 620, standard 1326.
- The root `data/kg/marine_pump/triples/KG_v1_validated` is the strict 208-record graph used by the RP2 v6 paper.
- The 620- and 1326-record graphs are terminology-governance experiment assets. They are not interchangeable with the 208-record RP2 teacher graph.
- RP2 v6 uses 40 controlled queries with known fault scope and diagnostic role. It is not open-ended fault identification, and its output is a single-role set of evidence-linked atomic suggestions rather than a complete diagnosis card.

## Current RP3 execution status (2026-09-18)

- The model-capable server checkout is `~/08-zxk/Fault_LLM_V2`. Code is synchronized through `origin/main`; use Git pull/push rather than upload patch archives unless Git is unavailable.
- `TeacherGraph_RP3_v1` has been rebuilt and frozen on the server. Its compact memory reports 208 evidence records, 203 claims, 14 documents, 10 source families and 38 fault-role buckets. The 40 original controlled queries are split into 32 build-train and 8 group-disjoint build-validation traces.
- MP008 development preparation completed all 40 verifier rows. Training and MP008 feature bundles were generated separately with explicit `training` and `development` purposes.
- The baseline `lec_v1` completed training, route fitting, FP32 ONNX export and static INT8 QDQ quantization. The quantized controller has 40 QDQ nodes and was checked on 40 MP008 representative inputs.
- Baseline route rollouts show teacher-relative exact selection agreement of 11/32 on build-train and 1/8 on build-validation. Route targets are train: 11 answer, 16 fallback, 5 abstain; validation: 1 answer, 6 fallback, 1 abstain.
- Baseline post-INT8 MP008 calibration failed closed: raw route argmax counts are 26 fallback and 14 abstain, with zero answer predictions and zero feasible accepted answers. No deployable `calibration_manifest.json` exists, so baseline `evaluate` and edge deployment are blocked by design.
- The prespecified build-set evidence-removal arm has now completed augmentation (40 original + 125 derived traces; 129 train/36 validation), training, route fitting, FP32 export and INT8 quantization in `results/experiments/research_point_3/lec_augmented_v1`. Do not rerun these stages. Preserve both this arm and `lec_v1`.
- Augmented calibration also failed closed: all 40 MP008 raw route predictions are abstain, with zero accepted answers and no deployment certificate. Original-query nonempty exact teacher-selection success is 3/32 train and 0/8 validation (baseline 11/32 and 1/8). All 24 teacher-nonempty augmented validation rows predict requested-role cardinality zero.
- MP008 has only 3 teacher-nonempty answers and 37 empty sets. Under the current nonempty-answer contract, accepting at least 5 answers implies at least 40% teacher-set disagreement, so the frozen minimum-5/maximum-10% calibration constraints are infeasible even with perfect selection. Do not lower the gate or use external evaluation to fill calibration coverage.
- The augmented negative result requires build-only cardinality/model-selection investigation and an explicit calibration-protocol review. The head-ablation and field-normalization follow-ups are now complete (see below). See `docs/RP3_AUGMENTED_V1_RESULTS_20260917.md`. The augmented run used torch 2.6.0+cu118 versus baseline cu124, so runtime equivalence must not be claimed.
- During this continuation, 691 missing and 6 older experiment files were recovered from local commit `9370860`, with the 6 older versions backed up under `logs/rp3_asset_recovery_20260917T000825/`. All recovered bytes and baseline binaries were verified. A stale rebase was quit after metadata backup; work continues on `codex/rp3-augmented-v1-results`, with the original `main` commit preserved. Do not revert recovered tracked assets during Git synchronization.
- MP008 must not be used to change model hyperparameters, gradients, early stopping or model selection. The augmented arm keeps the baseline model/training settings and uses only prespecified build-set interventions with frozen 7B replay.
- P1 offline diagnostics and R0/R1/R2/R3 routing replays have completed for existing B0/M0 FP32/INT8 artifacts; see `docs/RP3_DIAGNOSTICS_V1.md`. `diagnose` reads build examples only and never creates a deployment certificate or bypasses `evaluate`.
- Three-seed A1-A4 head ablations completed under `configs/research_point_3/head_ablation_v1.json` (seeds 7042027/7042028/7042029). A4 reuses A3 bootstrap and fits only Route; all non-route weights and pointer selections were verified identical. Results are in `results/experiments/research_point_3/head_ablation_v1/`; do not rerun completed arms.
- Original-validation nonempty exact successes are A1: 2/8, 2/8, 2/8; A2: 2/8, 3/8, 2/8; A3/A4: 0/8 for all three seeds. The ablation chain disables composite intervention/Brier auxiliary losses equally to prevent supervision leakage into disabled heads; A4 is not an M0 repeat. These are build-only internal diagnostics with two validation scenario groups, not independent external accuracy.
- The prespecified three-seed field-normalization comparison completed in `field_normalization_v1`, reusing A3 all-field controls without retraining. Requested/nonrequested field-state and cardinality CE were separately normalized with 0.5/0.5 group weights; all other learning settings remained fixed. Original-validation scenario-macro seed means improved: nonempty raw-zero rate 91.67% to 37.50%, requested raw cardinality accuracy 16.67% to 37.50%, Pointer F1 0.0556 to 0.3472. However seed 7042027 increased validation-all teacher-empty false fill from 0/12 to 3/12. The prespecified overall improvement rule FAILED; do not promote this arm or select a different epoch after seeing these metrics. Best epochs are 2/1/1. See `docs/RP3_FIELD_NORMALIZATION_V1_RESULTS_20260917.md`; do not rerun completed controls or treatments.
- Supervision audit found 82.17% zero-cardinality training slots but only about 29–35% of best-checkpoint cardinality CE from nonrequested slots. Label frequency does not establish loss/gradient dominance. All A1/A2/A3 controls chose epoch 1 because rising validation ranking loss outweighed other loss reductions; A1/A2 had no field loss. New telemetry is diagnostic only, not a new selection rule.
- Joint gating audit and the prespecified W1/J1 three-seed experiment have completed in `joint_contract_v1`; C0 reuses the prior requested-balanced checkpoints. W1 adds hard-empty support supervision (weight 0.25); J1 also adds a support/field/cardinality semantic constraint (weight 0.10), with a capped Poisson-binomial probability calculation. Only 5/129 train rows are assessed teacher-empty cases with available candidates (validation 4/36). See `docs/RP3_JOINT_CONTRACT_V1_RESULTS_20260917.md` and `docs/research/RP3_JOINT_CONTRACT_METHOD.md`.
- W1/J1 both removed observed validation teacher-empty false fills, but original-validation mean Pointer F1 fell from C0 0.3472 to 0.0750; decoded nonempty-target empty rate rose from 41.67% to 90.28%. J1 lowered model violation mass versus W1 (0.4878 to 0.4690) and passed the narrow prespecified raw-metric comparison, but all validation selections remained identical to W1 and both arms failed improvement versus C0. Do not claim an overall algorithm gain or promote these checkpoints. Best epochs for both are 1/1/2. 123 RP3 tests passed; 1265 pre-existing research/log files were preserved.
- Build-only support separability/gradient audit completed in `support_separability_v1`. Identical pointwise support inputs have conflicting teacher labels in 10 train and 3 validation groups (empirical minimum errors 10/756 and 3/126); none of the 5 training hard-empty rows belong to those conflicts. C0 positive/hard-negative gradient cosine is -0.7242/-0.8266/-0.8018 at saved checkpoints; this is local full-train diagnosis, not a reconstruction of historical AdamW steps.
- The prespecified G1 actual-AdamW-step positive-support guard completed three seeds in `positive_guard_v1`, reusing C0/W1 without training. It projects proposed parameter displacement onto a train-positive descent halfspace and checks actual reference loss, with rollback if needed. G1/W1 passes the prespecified mean-quality/false-fill comparison: original-validation F1 0.0750 to 0.1972, decoded empty 90.28% to 55.56%, validation-all teacher-empty false fill still 0/12 per seed. G1/C0 FAILS (C0 F1 0.3472, decoded empty 41.67%). All G1 best epochs are 1, and all original-validation negative support labels pass the 0.5 support gate (15/15 per seed); do not call 100% positive recall a discrimination gain or promote G1. See `docs/RP3_POSITIVE_GUARD_V1_RESULTS_20260917.md`.
- G1 correctness checks passed: 129 RP3 tests, objective/checkpoint/update checks, and all 1331 prior research/log files preserved. Actual positive CE decreased in all 162 updates, but the constraint pushed both positive and negative logits upward. Do not stack G1, change teacher labels, raise negative weights blindly, or lower support thresholds.
- S0/S1 matched-capacity candidate-context experiment completed in `set_context_v1`: six new runs, each 1,409,222 parameters, with paired original weights/RNG and zero initial residual. S0 uses singleton context; S1 uses available-set mean/count. Both best epochs are 2/1/1. Every validation selection remains identical to C0, mean original-validation F1 0.3472; S1 negative-support false-accept rate increased from C0 0.6333 to 0.6500 (scenario macro). Both prespecified comparisons FAIL. S1 responds to all 10 train/3 validation pointwise conflict groups, so observability works without task improvement. Do not rerun or retune this architecture chain. See `docs/RP3_SET_CONTEXT_V1_RESULTS_20260917.md`.
- New `scope_feasibility_v1` is a read-only oracle ceiling, not a routing policy. With P teacher-nonempty tasks, G exact nonempty local proposals and b full-teacher calls, zero-error service is at most min(P,G+b). C0/S1 original validation has P=7, G=2/2/0, requiring at least 5/5/7 full-teacher calls even for an oracle. A pure routing pivot is therefore not established. 137 RP3 tests passed and 1388 pre-existing research/log files were preserved.
- E1 frozen BGE-small-zh-v1.5 and matched-dimension H512 completed three seeds each in `semantic_v1`; C0 reused read-only. Both prespecified overall comparisons FAIL: original-validation scenario-macro F1 C0/H512/E1 = 0.3472/0.2819/0.2792; E1 seed 7042027 teacher-empty false fills 4/12 versus C0 3/12. E1 exact nonempty proposals G=2/1/0 do not improve C0 2/2/0. Total E1 parameters including encoder = 25,411,621; CPU FP32 query-encoding/assembly/controller/decoding mean validation latency = 82.342 ms versus C0 4.812 ms, not end-to-end or edge-device results. See `docs/RP3_SEMANTIC_V1_RESULTS_20260918.md`; do not rerun E1/H512 or search more encoders under this protocol.
- E1 correctness checks passed: 141 RP3 tests, identical labels/splits/metadata, objective and early-stopping reconstruction, all-head unavailable-input invariance, online/offline features and selections on 40 original queries, and preservation of 1457 prior research/log files. Original child ru_maxrss had a shared high-water floor; preserve it but use separate `/proc` memory audits for process-memory comparison. No model, threshold or deployment promotion occurred.
- Next: review build-only task/supervision alignment, independent scenario/query-family coverage, and a falsifiable RP3 contribution before resuming algorithm experiments. The semantic search is stopped and a new learned router contingent on E1 gains is not started. Encoder substitution, generic routing formulas and elementary oracle bounds are not themselves original algorithm contributions. Scope adjustment and calibration review must be explicit; current MP008 gates, old failure reports, teacher/data boundaries and external-evaluation exclusion remain unchanged. B0/M0 multiseed expansion, remaining intervention controls and target hardware are still pending.

## Research point 3 gates

1. Freeze one explicit `TeacherGraph_RP3_v1` on the strict 208-record graph actually used by RP2 v6, with input paths, hashes, 208 evidence/203 claim/281 entity counts, vector index, model versions, and replay results. The 620/1326 layers are post-freeze tier-shift stress tests, not alternative main teachers.
2. Rebuild and replay the RP2 selector, verifier, and abstention policy on that exact graph before exporting teacher traces. This has completed on the model-capable server; other checkouts must still verify the frozen manifests and must not synthesize missing assets.
3. Freeze a complete diagnosis-card schema. Do not call the existing RP2 single-role JSON a complete card.
4. The main student is a four-head LEC (rank/pointer, binary support, field/underfill, three-action route), target <50M parameters and ONNX INT8. Keep deterministic text rendering as a rule component.
5. Qwen2.5-3B-Instruct INT4 is an optional edge dialogue/tool-calling shell; 1.5B and 7B are shell capacity bounds, not the main distilled object. Thresholds must be recalibrated after any quantization.
6. Evidence-removal/interference training requires the student to observe a bounded candidate signature or availability mask. A fully closed-book student cannot claim evidence-set intervention sensitivity.
7. Online actions are student answer, full RP2 teacher fallback, or abstain/human review. An ID-addressed local evidence memory is bounded evidence access, not retrieval-free inference.
8. Do not motivate RP3 by claiming the graph cannot fit on the edge; the evidence store is small. Measure savings from avoiding embedding, candidate scoring, long prompts, and repeated 7B verification.
9. A real edge-deployment claim requires measurements on target hardware. RTX 5880 resource limiting is only a resource-constrained simulation.
10. Build-set group-disjoint validation is used for early stopping/model selection. MP008 is reserved only for support/route thresholds and post-quantization calibration; it must not contribute gradients, early stopping, or model selection.

## Preservation and cleanup

- Never delete original PDFs, parsed pages, real API responses/caches, candidate decisions and failure reasons, frozen graph assets, frozen experiment outputs, or the two paper source trees.
- Generated render/QA caches and LaTeX build intermediates may be deleted when the final source and PDF are preserved.
- Preserve existing user modifications in a dirty worktree. In particular, do not overwrite or move an already modified paper draft.
- Before destructive cleanup, resolve explicit absolute targets inside the repository and verify that they are reproducible caches.
- Server-generated teacher caches, MP008 responses, checkpoints, ONNX files, calibration-search reports and experiment logs are research records. Do not remove them when synchronizing code with Git.
